import { Typography } from "@mui/material";

const NotFound = () => {
  return (
    <Typography variant="h5" component="h5" sx={{ p: 5 }}>
      Page not found :(
    </Typography>
  );
};

export default NotFound;
